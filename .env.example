# Optional local environment file (gitignored: .env, .env.*).
# The current SoulPod server reads runtime settings from config/app_runtime.json
# or the /settings page. Use this file only if you later wire dotenv or external tools.

# Template DigitalTwinPackage

Copy this directory, rename it, and replace placeholder content. Required files for `load_soul_package()`:

- `profile.json`
- `config.json`
- `system_prompts.txt`

Optional: create `memories/` and `assets/` as needed.
